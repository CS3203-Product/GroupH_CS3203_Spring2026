/**
 * Distraction Blocker — API origins (no trailing slashes).
 * Set prod to your deployed FastAPI origin before publishing the extension.
 */
self.DISTRACTION_BLOCKER_API_BASES = Object.freeze({
  dev: 'http://localhost:8000',
  prod: 'https://grouph-cs3203-spring2026-h2lk.onrender.com/',
});
